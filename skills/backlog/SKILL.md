---
name: backlog
description: Show open friction points, active commitments, priorities, and last session status from WORKING_AGREEMENT.md
disable-model-invocation: false
---

Run the backlog script and show the output:

```bash
${CLAUDE_PLUGIN_ROOT}/scripts/backlog.sh
```

Show the output as-is. No commentary unless the user asks.

$ARGUMENTS
