# type(scope): summary
# Types: feat | fix | refactor | docs | test | chore | session | decide

Why:


Before:


State:
Next:
Active:

Rejected:


Assumes:


Fragile:


Discovered:


Open:


# ─────────────────────────────────────────────────────────────────
# Required: header line, Why, State/Next
# Optional: all others — delete sections that don't apply
#
# Before:    — what was the behavior/state before this change?
# Rejected:  — alternatives tried or considered and why they didn't work
# Assumes:   — invariants this change relies on; if broken, this breaks
# Fragile:   — non-obvious gotchas; things that look wrong but are intentional
# Discovered — non-obvious findings made during this work
# Open:      — unresolved decisions or questions
#
# Scaffold: this template is the human-facing scaffold (installed as ~/.gitmessage).
# The parallel programmatic scaffold for Claude's commits is scripts/create-commit.sh.
# Both produce the same structure. If the format changes, update both.
# ─────────────────────────────────────────────────────────────────
